import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.korpr8.app',
  appName: 'Korpr8',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
